package org.acme.features.market.verify.domain.model.valueobject;

import org.acme.common.exception.ConstraintException;
import org.acme.features.market.medal.domain.model.MedalReference;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class VerifyMedalMedalVOUnitTest {

  /**
   * @autogenerated ValueObjectGenerator
   */
  @Test
  @DisplayName("Test value object contruction for property medal of verify medal ")
  void test_verify_medal_medal_v_o_builder() {
    Assertions.assertThrows(ConstraintException.class, () -> VerifyMedalMedalVO.tryFrom(null));
    Assertions.assertEquals(MedalReference.of("one"),
        VerifyMedalMedalVO.from(MedalReference.of("one")).getValue());
    Assertions.assertThrows(ConstraintException.class,
        () -> VerifyMedalMedalVO.tryFrom(new Object() {}));
  }
}
