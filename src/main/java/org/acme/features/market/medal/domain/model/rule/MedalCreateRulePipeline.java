package org.acme.features.market.medal.domain.model.rule;

import java.util.Iterator;

import org.acme.features.market.medal.domain.model.Medal;

public interface MedalCreateRulePipeline {

  /**
   * @autogenerated AggregateGenerator
   * @param rules
   * @param medal
   * @return
   */
  public static Medal fire(Iterator<MedalCreateRulePipeline> rules, Medal medal) {
    Medal result = medal;
    while (rules.hasNext()) {
      result = rules.next().creating(result);
    }
    return result;
  }

  /**
   * @autogenerated AggregateGenerator
   * @param medal
   * @return
   */
  Medal creating(Medal medal);
}
