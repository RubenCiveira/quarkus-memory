package org.acme.features.market.fruit.application.allow;

import java.util.Optional;
import java.util.concurrent.CompletableFuture;

import org.acme.common.security.Allow;
import org.acme.features.market.fruit.application.interaction.query.FruitAllowQuery;

import lombok.Builder;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.NonNull;
import lombok.With;

@Data
@With
@Builder(toBuilder = true)
@EqualsAndHashCode(onlyExplicitlyIncluded = true)
public class FruitCreateAllow {

  /**
   * @autogenerated EntityGenerator
   * @param allowed
   * @return
   */
  public static FruitCreateAllow build(final boolean allowed) {
    return build(allowed, null);
  }

  /**
   * @autogenerated EntityGenerator
   * @param allowed
   * @param reason
   * @return
   */
  public static FruitCreateAllow build(final boolean allowed, final String reason) {
    return FruitCreateAllow.builder().detail(CompletableFuture
        .completedFuture(Allow.builder().allowed(allowed).description(reason).build())).build();
  }

  /**
   * @autogenerated EntityGenerator
   */
  private final FruitAllowQuery query;

  /**
   * @autogenerated EntityGenerator
   */
  @NonNull
  private CompletableFuture<Allow> detail;

  /**
   * @autogenerated EntityGenerator
   * @return
   */
  public Optional<FruitAllowQuery> getQuery() {
    return Optional.ofNullable(query);
  }
}
