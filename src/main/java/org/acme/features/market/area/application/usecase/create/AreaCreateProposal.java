package org.acme.features.market.area.application.usecase.create;

import org.acme.common.action.Interaction;
import org.acme.features.market.area.application.AreaDto;
import org.acme.features.market.area.application.service.proposal.AreaNewProposal;

import jakarta.enterprise.event.Event;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.SuperBuilder;

@Data
@SuperBuilder(toBuilder = true)
@EqualsAndHashCode(callSuper = true, onlyExplicitlyIncluded = true)
public class AreaCreateProposal extends AreaNewProposal {

  /**
   * @autogenerated CreateProposalGenerator
   * @param emitter
   * @param interaction
   * @param dto
   * @return
   */
  public static AreaDto resolveWith(final Event<AreaCreateProposal> emitter,
      final Interaction interaction, final AreaDto dto) {
    return resolveWith(emitter,
        AreaCreateProposal.builder().interaction(interaction).dto(dto).build());
  }
}
