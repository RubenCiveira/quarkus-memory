package org.acme.features.market.area.domain.model.rule;

import java.util.Iterator;

import org.acme.features.market.area.domain.model.Area;

public interface AreaDeleteRulePipeline {

  /**
   * @autogenerated AggregateGenerator
   * @param rules
   * @param area
   * @return
   */
  public static Area fire(Iterator<AreaDeleteRulePipeline> rules, Area area) {
    Area result = area;
    while (rules.hasNext()) {
      result = rules.next().deleting(result);
    }
    return result;
  }

  /**
   * @autogenerated AggregateGenerator
   * @param area
   * @return
   */
  Area deleting(Area area);
}
