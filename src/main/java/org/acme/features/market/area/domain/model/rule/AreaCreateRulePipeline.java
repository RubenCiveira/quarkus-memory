package org.acme.features.market.area.domain.model.rule;

import java.util.Iterator;

import org.acme.features.market.area.domain.model.Area;

public interface AreaCreateRulePipeline {

  /**
   * @autogenerated AggregateGenerator
   * @param rules
   * @param area
   * @return
   */
  public static Area fire(Iterator<AreaCreateRulePipeline> rules, Area area) {
    Area result = area;
    while (rules.hasNext()) {
      result = rules.next().creating(result);
    }
    return result;
  }

  /**
   * @autogenerated AggregateGenerator
   * @param area
   * @return
   */
  Area creating(Area area);
}
