package org.acme.features.market.area.application.usecase.delete;

import org.acme.common.action.Interaction;
import org.acme.features.market.area.application.service.proposal.AreaRemoveProposal;
import org.acme.features.market.area.domain.model.AreaRef;

import jakarta.enterprise.event.Event;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.SuperBuilder;

@Data
@SuperBuilder(toBuilder = true)
@EqualsAndHashCode(callSuper = true, onlyExplicitlyIncluded = true)
public class AreaDeleteProposal extends AreaRemoveProposal {

  /**
   * @autogenerated DeleteProposalGenerator
   * @param emitter
   * @param interaction
   * @param reference
   * @return
   */
  public static AreaRef resolveWith(final Event<AreaDeleteProposal> emitter,
      final Interaction interaction, final AreaRef reference) {
    return resolveWith(emitter,
        AreaDeleteProposal.builder().interaction(interaction).reference(reference).build());
  }
}
