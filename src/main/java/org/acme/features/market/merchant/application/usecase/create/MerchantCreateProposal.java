package org.acme.features.market.merchant.application.usecase.create;

import org.acme.common.action.Interaction;
import org.acme.features.market.merchant.application.MerchantDto;
import org.acme.features.market.merchant.application.service.proposal.MerchantNewProposal;

import jakarta.enterprise.event.Event;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.SuperBuilder;

@Data
@SuperBuilder(toBuilder = true)
@EqualsAndHashCode(callSuper = true, onlyExplicitlyIncluded = true)
public class MerchantCreateProposal extends MerchantNewProposal {

  /**
   * @autogenerated CreateProposalGenerator
   * @param emitter
   * @param interaction
   * @param dto
   * @return
   */
  public static MerchantDto resolveWith(final Event<MerchantCreateProposal> emitter,
      final Interaction interaction, final MerchantDto dto) {
    return resolveWith(emitter,
        MerchantCreateProposal.builder().interaction(interaction).dto(dto).build());
  }
}
