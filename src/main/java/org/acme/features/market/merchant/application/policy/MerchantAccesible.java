package org.acme.features.market.merchant.application.policy;

import org.acme.common.security.Actor;
import org.acme.features.market.merchant.application.service.proposal.MerchantVisibilityQueryProposal;

import jakarta.enterprise.event.Observes;

public class MerchantAccesible {

  /**
   * Constant for rOOT field
   *
   * @autogenerated VisibilityPolicyGenerator
   */
  private static final String ROL_ROOT = "ROOT";

  /**
   * @autogenerated VisibilityPolicyGenerator
   * @param filter
   */
  public void filterVisibles(@Observes MerchantVisibilityQueryProposal filter) {
    Actor actor = filter.getInteraction().getActor();
    if (!actor.hasRole(ROL_ROOT)) {
      filter.peek(fl -> fl.setMerchantAccesible(actor.getTenant().orElse(null)));
    }
  }
}
