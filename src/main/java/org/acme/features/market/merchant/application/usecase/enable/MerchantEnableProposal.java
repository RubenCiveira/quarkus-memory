package org.acme.features.market.merchant.application.usecase.enable;

import org.acme.common.action.Interaction;
import org.acme.features.market.merchant.application.MerchantDto;
import org.acme.features.market.merchant.application.service.proposal.MerchantModifyProposal;
import org.acme.features.market.merchant.domain.model.Merchant;

import jakarta.enterprise.event.Event;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.SuperBuilder;

@Data
@SuperBuilder(toBuilder = true)
@EqualsAndHashCode(callSuper = true, onlyExplicitlyIncluded = true)
public class MerchantEnableProposal extends MerchantModifyProposal {

  /**
   * @autogenerated ActionProposalGenerator
   * @param emitter
   * @param interaction
   * @param dto
   * @param entity
   * @return
   */
  public static MerchantDto resolveWith(final Event<MerchantEnableProposal> emitter,
      final Interaction interaction, final MerchantDto dto, final Merchant entity) {
    return resolveWith(emitter,
        MerchantEnableProposal.builder().interaction(interaction).dto(dto).entity(entity).build());
  }
}
