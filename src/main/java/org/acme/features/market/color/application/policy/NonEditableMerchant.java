package org.acme.features.market.color.application.policy;

import org.acme.common.security.Actor;
import org.acme.features.market.color.application.service.proposal.ColorFixedFieldsProposal;

import jakarta.enterprise.event.Observes;

public class NonEditableMerchant {

  /**
   * Constant for rOOT field
   *
   * @autogenerated VisibilityPolicyGenerator
   */
  private static final String ROL_ROOT = "ROOT";

  /**
   * @autogenerated VisibilityPolicyGenerator
   * @param fixed
   */
  public void fixedFields(@Observes ColorFixedFieldsProposal fixed) {
    Actor actor = fixed.getQuery().getActor();
    if (!actor.hasRole(ROL_ROOT)) {
      fixed.add("merchant");
    }
  }
}
