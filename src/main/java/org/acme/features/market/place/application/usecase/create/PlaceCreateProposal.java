package org.acme.features.market.place.application.usecase.create;

import org.acme.common.action.Interaction;
import org.acme.features.market.place.application.PlaceDto;
import org.acme.features.market.place.application.service.proposal.PlaceNewProposal;

import jakarta.enterprise.event.Event;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.SuperBuilder;

@Data
@SuperBuilder(toBuilder = true)
@EqualsAndHashCode(callSuper = true, onlyExplicitlyIncluded = true)
public class PlaceCreateProposal extends PlaceNewProposal {

  /**
   * @autogenerated CreateProposalGenerator
   * @param emitter
   * @param interaction
   * @param dto
   * @return
   */
  public static PlaceDto resolveWith(final Event<PlaceCreateProposal> emitter,
      final Interaction interaction, final PlaceDto dto) {
    return resolveWith(emitter,
        PlaceCreateProposal.builder().interaction(interaction).dto(dto).build());
  }
}
