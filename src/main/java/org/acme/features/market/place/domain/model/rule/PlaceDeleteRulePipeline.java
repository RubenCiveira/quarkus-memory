package org.acme.features.market.place.domain.model.rule;

import java.util.Iterator;

import org.acme.features.market.place.domain.model.Place;

public interface PlaceDeleteRulePipeline {

  /**
   * @autogenerated AggregateGenerator
   * @param rules
   * @param place
   * @return
   */
  public static Place fire(Iterator<PlaceDeleteRulePipeline> rules, Place place) {
    Place result = place;
    while (rules.hasNext()) {
      result = rules.next().deleting(result);
    }
    return result;
  }

  /**
   * @autogenerated AggregateGenerator
   * @param place
   * @return
   */
  Place deleting(Place place);
}
