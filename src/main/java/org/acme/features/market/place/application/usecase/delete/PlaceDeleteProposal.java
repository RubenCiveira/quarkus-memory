package org.acme.features.market.place.application.usecase.delete;

import org.acme.common.action.Interaction;
import org.acme.features.market.place.application.service.proposal.PlaceRemoveProposal;
import org.acme.features.market.place.domain.model.PlaceRef;

import jakarta.enterprise.event.Event;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.SuperBuilder;

@Data
@SuperBuilder(toBuilder = true)
@EqualsAndHashCode(callSuper = true, onlyExplicitlyIncluded = true)
public class PlaceDeleteProposal extends PlaceRemoveProposal {

  /**
   * @autogenerated DeleteProposalGenerator
   * @param emitter
   * @param interaction
   * @param reference
   * @return
   */
  public static PlaceRef resolveWith(final Event<PlaceDeleteProposal> emitter,
      final Interaction interaction, final PlaceRef reference) {
    return resolveWith(emitter,
        PlaceDeleteProposal.builder().interaction(interaction).reference(reference).build());
  }
}
