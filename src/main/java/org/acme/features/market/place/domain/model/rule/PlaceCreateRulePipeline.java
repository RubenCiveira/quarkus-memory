package org.acme.features.market.place.domain.model.rule;

import java.util.Iterator;

import org.acme.features.market.place.domain.model.Place;

public interface PlaceCreateRulePipeline {

  /**
   * @autogenerated AggregateGenerator
   * @param rules
   * @param place
   * @return
   */
  public static Place fire(Iterator<PlaceCreateRulePipeline> rules, Place place) {
    Place result = place;
    while (rules.hasNext()) {
      result = rules.next().creating(result);
    }
    return result;
  }

  /**
   * @autogenerated AggregateGenerator
   * @param place
   * @return
   */
  Place creating(Place place);
}
