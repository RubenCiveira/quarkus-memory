package org.acme.features.market.verify.application.usecase.delete;

import org.acme.common.action.Interaction;
import org.acme.features.market.verify.application.service.proposal.VerifyRemoveProposal;
import org.acme.features.market.verify.domain.model.VerifyRef;

import jakarta.enterprise.event.Event;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.SuperBuilder;

@Data
@SuperBuilder(toBuilder = true)
@EqualsAndHashCode(callSuper = true, onlyExplicitlyIncluded = true)
public class VerifyDeleteProposal extends VerifyRemoveProposal {

  /**
   * @autogenerated DeleteProposalGenerator
   * @param emitter
   * @param interaction
   * @param reference
   * @return
   */
  public static VerifyRef resolveWith(final Event<VerifyDeleteProposal> emitter,
      final Interaction interaction, final VerifyRef reference) {
    return resolveWith(emitter,
        VerifyDeleteProposal.builder().interaction(interaction).reference(reference).build());
  }
}
