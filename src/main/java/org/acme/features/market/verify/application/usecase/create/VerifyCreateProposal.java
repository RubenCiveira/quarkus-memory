package org.acme.features.market.verify.application.usecase.create;

import org.acme.common.action.Interaction;
import org.acme.features.market.verify.application.VerifyDto;
import org.acme.features.market.verify.application.service.proposal.VerifyNewProposal;

import jakarta.enterprise.event.Event;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.SuperBuilder;

@Data
@SuperBuilder(toBuilder = true)
@EqualsAndHashCode(callSuper = true, onlyExplicitlyIncluded = true)
public class VerifyCreateProposal extends VerifyNewProposal {

  /**
   * @autogenerated CreateProposalGenerator
   * @param emitter
   * @param interaction
   * @param dto
   * @return
   */
  public static VerifyDto resolveWith(final Event<VerifyCreateProposal> emitter,
      final Interaction interaction, final VerifyDto dto) {
    return resolveWith(emitter,
        VerifyCreateProposal.builder().interaction(interaction).dto(dto).build());
  }
}
