# @autogenerated
quarkus:
  application:
    name: test
  banner:
    path: banner.txt
  config:
    locations:
      # - config/dev-properties.yaml
      - config/server.yaml
      - config/telemetry.yaml
      - config/security.yaml
      - config/database.yaml
      - config/cache.yaml
      - META-INF/build/native.yaml
  http:
    port: 8090

