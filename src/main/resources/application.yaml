# @autogenerated
quarkus:
  application:
    name: test
  banner:
    path: banner.txt
  config:
    locations:
      - config/server.yaml
      - config/telemetry.yaml
      - config/security.yaml
      - config/database.yaml
  ssl:
    native: true
  http:
    port: 8090
#  native:
#    additional-build-args: "--initialize-at-run-time=io.opentelemetry.api.GlobalOpenTelemetry"
    