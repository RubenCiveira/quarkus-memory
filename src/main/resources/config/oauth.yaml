# @autogenerated
masterpassword: sesamo
oauth:
  jwt:
    authorization:
      issuer: http://localhost:${quarkus.http.port}/oauth/openid/
      store-pass: zfqvjnxl
      store-pair: jwpair
      key-duration: 3d
      futures-key: 5

    expiration:
      refresh-token: 20m
      mfa-token: 3m
      access-token: 5m
  client-app:
    username: green
    password: toop

quarkus:
  http:
    filter:
      oauth-themes:
        header:
          Cache-Control: public, max-age=31536000, s-maxage=31536000
        methods:
        - GET
        - HEAD
        matches: /oauth/themes/.*
      oauth-assets:
        header:
          Cache-Control: public, max-age=100, s-maxage=100
        methods:
        - GET
        - HEAD
        matches: /oauth/assets/.*
